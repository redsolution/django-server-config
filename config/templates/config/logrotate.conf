/var/log/{{ project_name }}/*.log {
        weekly
        missingok
        copytruncate
        rotate 12
        compress
        notifempty
        sharedscripts
        postrotate
                kill -HUP $(cat /var/run/{{ project_name }}/pid) > /dev/null 2>&1
        endscript
}
